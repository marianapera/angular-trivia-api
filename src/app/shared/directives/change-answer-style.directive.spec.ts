import { ChangeAnswerStyleDirective } from './change-answer-style.directive';

describe('ChangeAnswerStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeAnswerStyleDirective();
    expect(directive).toBeTruthy();
  });
});
